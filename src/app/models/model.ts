interface HeroSlide {
  image: string;
  eyebrow: string;
  title: string;
  accent: string;
  description: string;
  primaryText: string;
  secondaryText: string;
}

 interface CtaFeature {
  icon: string;
  title: string;
  description: string;
}

interface AboutService {
  icon: string;
  title: string;
  description: string;
}

interface AboutStat {
  value: string;
  label: string;
}

interface Destination {
  image: string;
  region: string;
  title: string;
  description: string;
  icon: string;
}

interface HomeTour {
  slug: string;
  image: string;
  category: string;
  location: string;
  title: string;
  duration: string;
  price: string;
  description: string;
  icon: string;
}

